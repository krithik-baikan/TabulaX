{
  "name": "tabulax-full",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently \"npm run frontend\" \"npm run nodeapi\"",
    "frontend": "cd tabulax-ui && npm start",
    "nodeapi": "cd tabulax-ui/server && node index.js"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}